import { userRepository } from "src/repositories/user.repository";
import { ChatService } from "./chat"

describe('', () => {

    let service: ChatService;

    beforeEach(() => {
        mockUserRepo: UserRepository = {} as any;
        service = new ChatService()
    })

    it('user joins', () => {
        
    })

})