SERVER_PORT=4000

# PostgreSQL with PostGIS extension
DATABASE_URL=postgresql://pg:password@localhost:5432/geochat?schema=public

# Auth
JWT_SECRET=change-me-in-production-please
JWT_EXPIRES_IN=7d

# CORS — comma-separated list (or *) of allowed web origins
CORS_ORIGIN=http://localhost:5180,http://localhost:3000,http://localhost:4173
